[tool.poetry]
name = "aqa web-patterns"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
selenium = "^4.38.0"
pytest = "^8.4.2"
webdriver-manager = "^4.0.2"
faker = "^37.12.0"
allure-pytest = "^2.15.0"
python-dotenv = "^1.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
